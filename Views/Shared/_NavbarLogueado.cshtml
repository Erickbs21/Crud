@{
    var nombreUsuario = Context.Session.GetString("NombreCompleto") ?? "Usuario";
    var rol = Context.Session.GetString("Rol") ?? "Usuario";
}

<header>
    <nav class="navbar navbar-expand-lg navbar-dark navbar-futurista">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center text-white" asp-controller="Panel" asp-action="Index">
                <i class="bi bi-hexagon-fill text-primary me-2"></i>
                <span class="fw-bold">NeoGestión</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link text-white" asp-controller="Panel" asp-action="Index">
                            <i class="bi bi-grid-3x3-gap me-1"></i> Panel
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" id="apiDropdown" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-cloud me-1"></i> APIs
                        </a>
                        <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="apiDropdown">
                            <li>
                                <a class="dropdown-item" asp-controller="Api" asp-action="Rest">
                                    <i class="bi bi-arrow-repeat me-1"></i> API REST
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" asp-controller="Api" asp-action="Soap">
                                    <i class="bi bi-envelope me-1"></i> API SOAP
                                </a>
                            </li>
                        </ul>
                    </li>
                    @if (rol == "Administrador")
                    {
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-white" href="#" id="adminDropdown" role="button"
                               data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-shield-lock me-1"></i> Administración
                            </a>
                            <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="adminDropdown">
                                <li>
                                    <a class="dropdown-item" asp-controller="Panel" asp-action="Bitacora">
                                        <i class="bi bi-journal-text me-1"></i> Bitácora
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" asp-controller="Panel" asp-action="Errores">
                                        <i class="bi bi-exclamation-triangle me-1"></i> Registro de Errores
                                    </a>
                                </li>
                            </ul>
                        </li>
                    }
                </ul>
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center text-white" href="#" id="userDropdown" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="avatar-circle me-2">
                                <span class="avatar-text">@nombreUsuario.Substring(0, 1).ToUpper()</span>
                            </div>
                            <span>@nombreUsuario</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-dark dropdown-menu-end" aria-labelledby="userDropdown">
                            <li>
                                <div class="dropdown-item-text text-muted small">
                                    <i class="bi bi-person-badge me-1"></i> @rol
                                </div>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <form asp-controller="Cuenta" asp-action="Logout" method="post">
                                    <button type="submit" class="dropdown-item text-danger">
                                        <i class="bi bi-box-arrow-right me-1"></i> Cerrar Sesión
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>
